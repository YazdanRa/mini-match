paths-ignore:
  # This is public, non-secret Firebase client configuration. The API key only
  # identifies the project; bundle-ID and API restrictions limit its use, while
  # authentication, server authorization, and Security Rules control data access.
  # Never place server credentials or private keys at this excluded path.
  - "apps/apple/MiniMatch/MiniMatch/Resources/GoogleService-Info.plist"
